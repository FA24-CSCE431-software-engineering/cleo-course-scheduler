<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cleo Course Scheduler</title>
</head>

<h1>You're logged in!</h1>
<%= link_to "Sign Out", destroy_student_login_session_path %>

<h1> Dashboard </h1>
<table>
  <thead>
    <tr>
      <th>Table</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @models.each do |model| %>
      <tr>
        <td><%= model  %></td>
        <td>
          <a href="<%= send("#{model.to_s.underscore}s_path") %>">READ</a>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</html>
