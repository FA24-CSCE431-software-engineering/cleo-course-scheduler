
<div>
  <%= link_to 'Back to Home', admin_dashboard_path %>
</div>
<h1>Students</h1>

<table>
  <thead>
    <tr>
      <th>UIN</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th>Enrollment Year</th>
      <th>Graduation Year</th>
      <th>Enrollment Semester</th>
      <th>Graduation Semester</th>
      <th>Major</th> <!-- New column for Major -->
      <th>Emphasis</th>
      <th>Track</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.id %></td>
        <td><%= student.first_name %></td>
        <td><%= student.last_name %></td>
        <td><%= student.email %></td>
        <td><%= student.enrol_year %></td>
        <td><%= student.grad_year %></td>
        <td><%= student.enrol_semester %></td>
        <td><%= student.grad_semester %></td>
        <td><%= student.major.present? ? student.major.mname : 'N/A' %></td> 
        <td><%= student.emphasis.present? ? student.emphasis.ename : 'N/A' %></td> 
        <td><%= student.track.present? ? student.track.tname : 'N/A' %></td> 
        <td>
          <%= link_to 'Edit', edit_student_path(student.google_id) %> 
          <%= link_to 'Delete', confirm_destroy_student_path(student.google_id), method: :get %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div>
  <%= link_to 'Add student', new_student_path %>
</div>

